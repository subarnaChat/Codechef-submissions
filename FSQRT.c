#include <stdio.h>
#include<math.h>
int main(void) {
    int t,n;
    scanf("%d",&t);
    while(t--)
    {
        scanf("%d",&n);
        printf("%d \n",(int)sqrt(n));
    }
	// your code goes here
	return 0;
}

